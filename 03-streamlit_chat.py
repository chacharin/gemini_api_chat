import streamlit as st
import google.generativeai as genai

# ---------------- ตั้งค่าหน้าเว็บและแอปพลิเคชัน ------------------
st.set_page_config(
    page_title="แชทบอทสุดยอดนักปลูก 🌱",  # กำหนดชื่อหน้าเว็บที่แสดงบนเบราว์เซอร์
    page_icon="🌱",  # กำหนดไอคอนที่แสดงบนแท็บเบราว์เซอร์
    layout="centered",  # กำหนดให้เนื้อหากลางหน้าจอ
)

st.title("🌱 แชทบอทสุดยอดนักปลูก")  # แสดงหัวข้อหลักของแอป
st.caption("ให้คำแนะนำการปลูกพืชผักสวนครัวด้วย Gemini 1.5-Flash")  # แสดงคำอธิบายสั้น ๆ เกี่ยวกับแอป

# ---------------- ตั้งค่า API Key (ฝังตรงนี้เพื่อความสะดวกแต่ไม่ปลอดภัย) ------------------
API_KEY = "คีย์ที่ได้จาก google ai studio"  # ⛔️ การฝังคีย์ตรงนี้ไม่ควรใช้ในโปรเจกต์จริงหรือโค้ดสาธารณะ

genai.configure(api_key=API_KEY)  # กำหนดค่า API Key ให้กับไลบรารีเพื่อเชื่อมต่อกับบริการ Gemini

# ---------------- ตั้งค่าโมเดล Gemini ----------------
## กำหนดบทบาทของโมเดลให้ตอบในฐานะเกษตรกรผู้เชี่ยวชาญ
SYSTEM_INSTRUCTION = (
    "คุณเป็นสุดยอดเกษตรกรผู้เชี่ยวชาญด้านการปลูกพืชผักสวนครัวไทย เช่น ผักบุ้ง กะเพรา โหระพา และอื่นๆ "
    "โปรดให้คำแนะนำและอธิบายแต่ละขั้นตอนอย่างละเอียด ใช้ภาษาสุภาพ เข้าใจง่าย เหมาะสำหรับผู้เริ่มต้น "
    "ห้ามแนะนำการใช้สารเคมีอันตรายหรือข้อมูลที่ผิดกฎหมาย "
    "หากผู้ใช้มีข้อสงสัยเพิ่มเติม ให้ชวนถามต่อหรือให้กำลังใจในการปลูก"
) 
## กำหนดค่าความสร้างสรรค์ของการตอบ
GENERATION_CONFIG = {"temperature": 0.7}  

# ---------------- สร้างโมเดลและอ็อบเจกต์แชท (ทำครั้งเดียวต่อ session) ----------------
if "chat" not in st.session_state:
    # สร้างอ็อบเจกต์โมเดลและกำหนดบทบาทพร้อมค่าคอนฟิก
    model = genai.GenerativeModel(
        model_name="gemini-1.5-flash",  # เลือกใช้โมเดล Gemini 1.5 Flash
        system_instruction=SYSTEM_INSTRUCTION,  # ส่งคำสั่งระบบให้โมเดล
        generation_config=GENERATION_CONFIG,  # ส่งค่าการสร้างข้อความ
    )
    # สร้างเซสชันแชทใหม่และเก็บไว้ใน session_state เพื่อคงประวัติการสนทนา
    st.session_state.chat = model.start_chat(history=[])

chat = st.session_state.chat  # ดึงอ็อบเจกต์แชทจาก session_state มาใช้งาน

# ---------------- แสดงข้อความสนทนาเดิม (ถ้ามี) ----------------
for turn in chat.history:
    role = "assistant" if turn.role == "model" else "user"  # กำหนดบทบาทสำหรับแต่ละข้อความ
    with st.chat_message(role):  # สร้างกล่องข้อความตามบทบาท
        st.markdown(turn.parts[0].text)  # แสดงข้อความแต่ละรอบจากประวัติ

# ---------------- กล่องรับข้อความจากผู้ใช้ ----------------
if prompt := st.chat_input("ถามคำถามการปลูกพืชผักที่นี่ แล้วกด Enter …"):
    # แสดงข้อความของผู้ใช้ทันทีในหน้าต่างสนทนา
    with st.chat_message("user"):
        st.markdown(prompt)

    # ส่งข้อความไปที่โมเดลและแสดงผลลัพธ์ที่ได้
    try:
        response = chat.send_message(prompt)  # ส่งข้อความไปยังโมเดลและรับคำตอบ
        with st.chat_message("assistant"):
            st.markdown(response.text)  # แสดงคำตอบของโมเดลในหน้าต่างสนทนา
    except Exception as err:
        st.error(f"เกิดข้อผิดพลาด: {err}")  # แสดงข้อผิดพลาดหากเกิดปัญหาระหว่างส่งข้อความ
